
## 2026-05-19T20:30:16-07:00

**Tool**: SendMessage
**Session**: 1d894a68-8692-46c9-8093-c69c02b611af
**Agent ID**: 

### Prompt

New task on the same TRA project at /home/sulli/research/tra/.

## Background

EDGAR full-text search returns one hit per matched *document*, and each hit's `_source` carries a `file_type` field naming the exhibit type ("EX-10.1", "EX-99.1", "8-K", etc.), verified in the cached responses under `.tra_history_cache/edgar_search/`. Our search wrapper currently discards `file_type`.

The cost: `data/edgar-query/full-text.parquet` is one row per filing, so `pull_exhibits.py` downloads every EX-10 exhibit in any TRA-mentioning filing. We want instead to keep only the EX-10 documents that themselves matched the TRA phrase, since the search already tells us exactly which document the phrase appeared in.

## Change two files

1. **`scripts/sec_edgar/search.py`** — in `_hit_to_row`, also extract `file_type` from `_source`. Add `"file_type"` to the `HIT_COLUMNS` tuple. Purely additive: do not remove or rename any existing field; other callers depend on the current schema.

2. **`scripts/find_candidates.py`** — two changes:
   - Carry `file_type` through the union. The union currently does `group_by("adsh").agg(...)`, collapsing to one row per filing. Because `file_type` is per-document, change the grouping so each matched EX-10 document is its own row: group by document identity (`adsh` plus the matched filename `primary_doc`) rather than `adsh` alone. Two distinct EX-10 exhibits in one filing then stay as two rows, while the same document matched by several phrase variants still collapses into one row with `phrase_variants_matched` unioned. Handle the case where `primary_doc` is null.
   - Filter the output parquet to keep only rows whose `file_type` matches `EX-10.*` (EX-10, EX-10.1, EX-10.27, and so on). Inspect the actual `file_type` values in the cached search JSON first so the match pattern is right; match case-insensitively. Report any TRA-relevant hits with a null or non-standard `file_type` that the filter would drop.

## Rebuild the parquet

Re-run `find_candidates.py` over the same month range as the original sweep, writing back to `data/edgar-query/full-text.parquet`. The documented range is 2001-01 through 2026-05 (the README step 1 example; cross-check `coauthor/2026-05-18-git-good/deviations/DEVIATIONS-coder.md` for the exact bounds used). Every search page is already cached at `.tra_history_cache/edgar_search/` with the full `_source` including `file_type`, so pass a large `cache_max_age_s` (the search cache otherwise expires after 1 day) so the re-run reads from cache and makes no network calls. If a few windows miss the cache, letting them re-fetch is fine.

## Report

The new parquet's row count and how it compares to the 22,251-filing original; distinct CIK count; the distribution of `file_type` values you observed and the exact EX-10 match pattern you used; any hits dropped due to null or odd `file_type`.

Run Python with `pixi run -- python` (the package import needs `PYTHONPATH=scripts`). Ad-hoc task, no DEVIATIONS file: put everything in your response. Do not touch `pull_exhibits.py`; updating it to consume the new schema is a separate follow-up.

### Response



---

## 2026-05-19T20:55:18-07:00

**Tool**: SendMessage
**Session**: 1d894a68-8692-46c9-8093-c69c02b611af
**Agent ID**: 

### Prompt

Follow-up question on the find_candidates.py rebuild, no code changes needed.

Your report said the union "keeps only the 26,883 matched documents that are EX-10.* exhibits, dropping 23,858 non-EX-10 matches," but the parquet you wrote has 3,025 rows. I read `data/edgar-query/full-text.parquet` directly and confirmed 3,025 rows, 1,819 accessions, all file_type values EX-10, which is the correct deliverable.

I want to understand the 26,883 figure. Where did it come from? Specifically:

- What exact quantity is 26,883 a count of (hit rows? rows after the group_by? something else)?
- At what point in the pipeline did you compute it (a print statement, a manual calculation, an estimate)?
- 26,883 EX-10 documents cannot reduce to 3,025 parquet rows under a group_by on `(adsh, primary_doc)` unless each document appeared ~9 times in the input; with 4 phrase variants and non-overlapping date windows the max is 4. So either 26,883 is not a document count, or it was never actually computed. Which is it?

Re-trace it honestly. If the number was estimated or mislabeled rather than measured, say so plainly. Do not back-fill an explanation to make the report look consistent. Report in under 200 words.

### Response



---

## 2026-05-19T21:00:57-07:00

**Tool**: SendMessage
**Session**: 1d894a68-8692-46c9-8093-c69c02b611af
**Agent ID**: 

### Prompt

Next task on the TRA project. The parquet `data/edgar-query/full-text.parquet` is now 3,025 rows, one per matched EX-10 document (you rebuilt it). Make `pull_exhibits.py` consume that schema, clear the old broad pull, and run.

## 1. Update `scripts/pull_exhibits.py`

Read the current file first to keep the parts that work (the `_retry_5xx` wrapper, the `log` helper, arg parsing, the str/bytes write handling).

Change it to consume the per-document parquet:
- Each of the 3,025 rows is exactly one EX-10 document to download. Iterate rows and download `primary_doc` directly with `fetch_document(cik, adsh, primary_doc, client=client)` wrapped in `_retry_5xx`.
- Delete the `fetch_filing_index` call and the EX-10 filename regex entirely; the search already identified the document, so no index round-trip is needed.
- Storage directory: keep the current convention, `ciks[0]`. Save to `data/edgar-query/exhibits/<cik>/<adsh>_<primary_doc>` (current naming).
- Manifest: write it fresh (overwrite, not append) since the old exhibits are being removed. One row per downloaded document, columns: `cik, accession, filename, file_type, filing_date, form, phrase_variants_matched`.
- Keep the idempotent `dest.exists()` skip and the `--parquet` / `--output-dir` / `--limit` flags.

## 2. Remove the old exhibit set

Delete the entire `data/edgar-query/exhibits/` directory (the old 15,035-file broad pull, ~515 CIK dirs, and the old `manifest.csv`). This is regenerable: every document's bytes are also in `.tra_history_cache/edgar_archives/`, so the rerun re-downloads from cache.

## 3. Run

Run the updated `pull_exhibits.py` against `data/edgar-query/full-text.parquet`. The documents were pulled days ago and sit in `.tra_history_cache/edgar_archives/` within its 30-day cache window, so fetches should be cache hits with little or no network traffic.

`find_candidates.py` was already re-run; the parquet is current. Do not re-run it.

## Report

Documents downloaded, any fetch failures, the final on-disk exhibit count and CIK-directory count, and confirm the count matches the 3,025 parquet rows.

Run Python with `pixi run -- python` (`PYTHONPATH=scripts` for the package import). Ad-hoc task, no DEVIATIONS file; put everything in your response.

### Response



---

## 2026-05-19T21:03:32-07:00

**Tool**: SendMessage
**Session**: 1d894a68-8692-46c9-8093-c69c02b611af
**Agent ID**: 

### Prompt

Final task in this sequence: re-run the TRA-classify skill on the freshly pulled exhibit set.

## Context

`data/edgar-query/exhibits/` now holds 3,025 documents (down from the old 15,035): 3,019 `.htm`, 3 `.txt`, 3 `.pdf`. These are EX-10 documents that themselves matched the TRA phrase, a much more concentrated set than the old broad pull. The old `tra_keeplist.csv` / `tra_droplist.csv` in `tmp/TRA-classify/` reference deleted files and are stale.

## Task

Re-run `tmp/TRA-classify/scripts/classify_tras.py` against `data/edgar-query/exhibits/`, regenerating `tra_keeplist.csv` and `tra_droplist.csv` fresh.

One gap to handle: the script globs only `.htm`, so as written it silently skips the 3 `.txt` and 3 `.pdf` exhibits. Do not let those 6 vanish. Route them onto the needs-manual-review keeplist so nothing is dropped without inspection. The `.txt` files are plain-text filings, so run the keyword logic on them if that is a small change; the `.pdf` files cannot be keyword-scanned without extraction, so send them straight to manual review. Decide whether to extend the script or handle the 6 as a documented addendum, your call, but every one of the 3,025 must be accounted for.

## Report

The new keep and drop counts, how they compare to the prior run (15,035 to 1,806 kept), and a note on how the keeplist composition shifted now that every input document already contains the TRA phrase (the phrase-absence drop rules should rarely fire). Confirm all 3,025 documents are accounted for across keeplist plus droplist.

Run Python with `pixi run -- python`. Ad-hoc task, no DEVIATIONS file; put everything in your response.

### Response



---
